# Example environment profile for PBR / IBL experiments.
# Runtime Save writes to config/environment_profile.local.ini, which is ignored by git.
hdrEquirectangularPath=
hdrTextureUnit=9
precomputeOnPrepare=0
