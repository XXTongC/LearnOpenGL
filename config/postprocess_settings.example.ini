# Example postprocess settings for HDR / Bloom experiments.
# Runtime Save writes to config/postprocess_settings.local.ini, which is ignored by git.
exposure=1.0
toneMappingMode=0
bloomEnabled=1
bloomThreshold=1.0
bloomIntensity=0.04
bloomIterations=6
