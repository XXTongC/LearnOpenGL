# Runtime frame pipeline pass plan and toggles.
# Copy to config/runtime_frame_pipeline.local.ini for local experiments.

passOrder=SceneColor,SceneResolve,Bloom,ScreenComposite
sceneColorPassEnabled=1
sceneResolvePassEnabled=1
bloomPassEnabled=1
screenCompositePassEnabled=1
